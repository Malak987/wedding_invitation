/// ============================================================
/// FONTS — Local Dashboard
/// ============================================================
/// Family names must match pubspec.yaml font declarations.
/// ============================================================

class AppFonts {
  AppFonts._();

  /// Used for elegant headings / couple names
  static const String heading = 'Playfair';

  /// Used for Arabic body text
  static const String body = 'Cairo';
}
